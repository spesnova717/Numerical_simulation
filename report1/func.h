double myFunc1(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*(1 - basic)*(basic*basic-basic-2);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc2(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*(1 - basic)*(basic*basic*basic-basic*basic-6*basic+2);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc3(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*(1 - basic)*(basic*basic*basic*basic-basic*basic*basic-12*basic*basic+6*basic);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc4(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*basic*(1 - basic)*(basic*basic-basic-2);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc5(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*basic*(1 - basic)*(basic*basic*basic-basic*basic-6*basic+2);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc6(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*basic*(1 - basic)*(basic*basic*basic*basic-basic*basic*basic-12*basic*basic+6*basic);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc7(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*basic*basic*(1 - basic)*(basic*basic-basic-2);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc8(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*basic*basic*(1 - basic)*(basic*basic*basic-basic*basic-6*basic+2);
    yyy = xxx * 1/2;
    return yyy;
}

double myFunc9(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = basic*basic*basic*(1 - basic)*(basic*basic*basic*basic-basic*basic*basic-12*basic*basic+6*basic);
    yyy = xxx * 1/2;
    return yyy;
}

double myFuncf1(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = -basic*(1-basic);
    yyy = xxx * 1/2;
    return yyy;
}

double myFuncf2(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = -basic*basic*(1-basic);
    yyy = xxx * 1/2;
    return yyy;
}

double myFuncf3(double x) {
    double xxx;
    double yyy;
    double basic;
    basic = (1 + x)/2;
    xxx = -basic*basic*basic*(1-basic);
    yyy = xxx * 1/2;
    return yyy;
}